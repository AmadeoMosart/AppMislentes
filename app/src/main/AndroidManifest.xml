<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.amadeo.appmislentes">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Vista.ViewBolsaDspacho.BolsaDespachoActivity"></activity>
        <activity android:name=".Vista.ViewAdicionales.MaterialActivity" />
        <activity android:name=".Vista.ViewAdicionales.TipoLenteActivity" />
        <activity android:name=".Vista.ViewAdicionales.PrescripcionActivity" />
        <activity android:name=".Vista.ViewAdicionales.TamanoActivity" />
        <activity android:name=".Vista.ViewFiltros.FiltrosActivity" />
        <activity android:name=".Vista.ViewProvador.PruebaActivity" />
        <activity android:name=".Vista.ViewContenedorPrincipal.ContenedorPrincipalActivity" />
        <activity android:name=".Vista.ViewBienvenida.LogoActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>